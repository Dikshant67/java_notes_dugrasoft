'1. Find out the Type of error
   ' Dim strName
   ' strName = "UFT"
   ' If isArray(strName)
   '      msgbox "It is  Array"
    'Else
    '     msgbox "It is not Array"
   ' End If
   
   Dim strName
   strName = "UFT"
   If isArray(strName)
        msgbox "It is  Array"
    Else
        msgbox "It is not Array"
   End If
   
   'Syntax error
   'Expected Then
   'Line 12
 